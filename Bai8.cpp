#include <iostream>
#include <cmath>

using namespace std;

/**int rnd(double n)
{
    int m;
    if (ceil(n) - n <= 0.5) m = ceil(n);
    else m = floor(n);
    return m;
}
*/

int rnd(double n)
{
    int m;
    
    return m;
}

int main(){
    double n;
    cin >> n;
    cout << rnd(n);
    return 0;
}
